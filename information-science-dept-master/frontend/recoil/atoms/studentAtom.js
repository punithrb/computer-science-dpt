import { atom } from "recoil";

export const studentAtom = atom({
  key: "studentAtom",
  default: null,
});

export const studentClassAtom = atom({
  key: "studentClassAtom",
  default: null,
});

export const studentAssignmentAtom = atom({
  key: "studentAssignmentAtom",
  default: null,
});